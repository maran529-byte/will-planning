apiVersion: v1
kind: Service
metadata:
  name: t1-compliance-engine
  namespace: aiwill-planner
spec:
  selector:
    app: t1-compliance-engine
  ports:
    - port: 8086
      targetPort: 8086
  type: ClusterIP